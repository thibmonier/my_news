<?php

declare(strict_types=1);

/*
 * Briefly AI — Configuration globale Pest 3.
 *
 * Les Feature tests utilisent explicitement :
 *   uses(Symfony\Bundle\FrameworkBundle\Test\WebTestCase::class);
 *
 * Les Unit tests n'ont pas besoin de TestCase (PHP pur).
 */

// Aucun uses() global — configuration par répertoire dans chaque fichier de test.
