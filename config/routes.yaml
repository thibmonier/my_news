## Briefly AI — Route definitions
## Presentation controllers discovered via PHP attributes.
## API Platform resources are auto-registered by the bundle loader.

controllers:
    resource:
        path: ../src/Presentation/Controller/
        namespace: App\Presentation\Controller
    type: attribute

api_platform:
    resource: .
    type: api_platform

# US-031 — Routes OAuth2 : définies par attributs #[Route] dans OAuthController
