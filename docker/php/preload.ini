; Briefly AI — OPcache preload (runtime PROD uniquement)
; Chargé après COPY du code dans le stage prod, quand /app/config/preload.php existe.
opcache.preload=/app/config/preload.php
opcache.preload_user=www-data
